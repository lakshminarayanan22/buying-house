__pycache__/
*.py[cod]
.venv/
.env
.env.local
node_modules/
.next/
dist/
build/
*.egg-info/
.pytest_cache/
.DS_Store

# Generated training data and downloaded adapters. Both are reproducible — the JSONL from
# scripts/build_classifier_dataset.py, the adapter from a Modal run — and neither is small.
backend/evals/training/
artifacts/

# Corrections in progress are committed (evals/sql_corrections.jsonl); the built dataset is not
# — it regenerates from them with scripts/build_sql_dataset.py.
